{/*

var express = require("express");
var router = express.Router();
var mysql = require("mysql");
var db = mongojs("mongodb://eman:eman@ds163181.mlab.com:63181/taxiapp", ["drivers"]);


//Get Single Driver
router.get("/livreur/:liv_id", function(req, res, next){
    db.livreur.findOne({_id: mysql.ObjectId(req.params.liv_id)},function(err, livreur){
        if (err){
            res.send(err);
        }
        res.send(livreur);
    });
});

module.exports = router;

*/}
